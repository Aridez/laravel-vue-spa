{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "npm run --prefix web-front serve",
    "build": "npm run --prefix web-front build",
    "lint": "npm run --prefix web-front lint",
    "install": "npm install --prefix web-front"
  }
}
